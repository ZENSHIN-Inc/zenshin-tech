# CLAUDE.md

このリポジトリの構成・運用ルール・スキル一覧は **[AGENTS.md](AGENTS.md) が単一情報源**。必ずそちらを参照すること。

- **commit / push はユーザーの明示指示があるまでしない**（push = 即公開）
- ビルドと検証: `bun run ci`（はみ出し自動検知込み）→ 目視は挿絵入りページのみ `dist/slides/<デッキ>.pdf` で確認
